package com.jtjmpm;

public enum MessageType {

    WELCOME,

    LOBBY_JOINED,
    CREATE_LOBBY,
    JOIN_LOBBY,
    LEAVE_LOBBY,
    DESTROY_LOBBY,
    LOBBY_DESTROYED,

    TOGGLE_READY,
    GAME_START,

    GET_SPELLS_LIST,
    AVAILABLE_SPELLS,

    PLAYER_MOVE,
    MOVE_RESULT,
    SHAPE_DRAWN,
    GAME_STATE_UPDATE,
    COMBAT_EVENT,
    GAME_OVER,
    RESTART_MATCH,

    ROUND_OVER,
    COUNTDOWN,
    ROUND_START,

    ERROR;
}
